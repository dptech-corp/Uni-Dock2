{% set name = "unidock2" %}

{% if cuda_compiler_version in (None, "None", True, False) %}
{% set cuda_major = 0 %}
{% else %}
{% set cuda_major = environ.get("cuda_compiler_version").split(".")[0] | int %}
{% endif %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  path: ..

build:
  number: {{ build }}
  string: cuda{{ cuda_major }}_py{{ PY_VER | replace(".", "") }}_{{ build }}

requirements:
  build:
    # - cross-python_{{ target_platform }}
    # - cython                                 # [build_platform != target_platform]
    - {{ compiler('c') }}
#    - {{ stdlib("c") }}
    - {{ compiler('cxx') }}
    - {{ compiler('cuda') }} # [(linux or win) and cuda_compiler != "None"]
    # BEGIN GENERATED: conda build tools
    - cmake >=3.27
    - make
    - pybind11
    # END GENERATED: conda build tools

  host:
    - python
    - pip
    - scikit-build-core >=1.0
    - pybind11
    - cuda-version {{ cuda_compiler_version }} # [(cuda_compiler_version or "None") != "None"]
    {% if cuda_compiler_version != "None" %}
    - libcurand-dev
    - cuda-cudart-dev
    - cuda-driver-dev  # [linux]
    {% endif %}
  run:
    - cuda-version >={{ cuda_major }},<{{ cuda_major + 1 }}
    - cuda-cudart
    # BEGIN GENERATED: conda run dependencies
    - numpy
    - networkx
    - pyyaml
    - pydantic
    - rdkit
    - openmm
    - pdbfixer
    - msys_viparr_lpsolve55
    - ambertools_stable
    # END GENERATED: conda run dependencies

test:
  imports:
    - unidock2
  commands:
    - unidock2 --version
    - unidock2 --help
    - python -c "from unidock2._engine import pipeline"
